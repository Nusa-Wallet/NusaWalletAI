# Pipeline entry points

This directory is reserved for reproducible data, training, and evaluation commands.
Importable business logic belongs under `app/`; scripts should only parse arguments
and invoke that logic. Implementations are scheduled for later phases.
